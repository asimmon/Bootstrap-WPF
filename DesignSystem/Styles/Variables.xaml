<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:ds="clr-namespace:DesignSystem"
                    xmlns:sys="clr-namespace:System;assembly=mscorlib">

    <ds:ColorLightnessConverter LightnessVariation="Lighten" x:Key="LightenConverter" />
    <ds:ColorLightnessConverter LightnessVariation="Darken" x:Key="DarkenConverter" />

    <ds:ArithmeticConverter Operation="Multiplication" x:Key="MultiplicationConverter" />

    <!-- Colors -->

    <SolidColorBrush x:Key="GrayBaseBrush" Color="#FF000000" />
    <SolidColorBrush x:Key="GrayDarkerBrush" Color="{Binding Color, Source={StaticResource GrayBaseBrush}, Converter={StaticResource LightenConverter}, ConverterParameter=13.5, Mode=OneTime}" />
    <SolidColorBrush x:Key="GrayDarkBrush" Color="{Binding Color, Source={StaticResource GrayBaseBrush}, Converter={StaticResource LightenConverter}, ConverterParameter=20, Mode=OneTime}" />
    <SolidColorBrush x:Key="GrayBrush" Color="{Binding Color, Source={StaticResource GrayBaseBrush}, Converter={StaticResource LightenConverter}, ConverterParameter=33.5, Mode=OneTime}" />
    <SolidColorBrush x:Key="GrayLightBrush" Color="{Binding Color, Source={StaticResource GrayBaseBrush}, Converter={StaticResource LightenConverter}, ConverterParameter=46.7, Mode=OneTime}" />
    <SolidColorBrush x:Key="GrayLighterBrush" Color="{Binding Color, Source={StaticResource GrayBaseBrush}, Converter={StaticResource LightenConverter}, ConverterParameter=93.5, Mode=OneTime}" />

    <SolidColorBrush x:Key="BrandPrimaryBrush" Color="#FF337AB7" />
    <SolidColorBrush x:Key="BrandSuccessBrush" Color="#FF5CB85C" />
    <SolidColorBrush x:Key="BrandInfoBrush" Color="#FF5BC0DE" />
    <SolidColorBrush x:Key="BrandWarningBrush" Color="#FFF0AD4E" />
    <SolidColorBrush x:Key="BrandDangerBrush" Color="#FFD9534F" />

    <SolidColorBrush x:Key="BodyBackgroundBrush" Color="#FFFFFFFF" />
    <SolidColorBrush x:Key="TextColorBrush" Color="{Binding Color, Source={StaticResource GrayDarkBrush}, Mode=OneTime}" />

    <SolidColorBrush x:Key="LinkColorBrush" Color="{Binding Color, Source={StaticResource BrandPrimaryBrush}, Mode=OneTime}" />
    <SolidColorBrush x:Key="LinkHoverColorBrush" Color="{Binding Color, Source={StaticResource LinkColorBrush}, Converter={StaticResource DarkenConverter}, ConverterParameter=15, Mode=OneTime}" />

    <!-- Text -->

    <FontFamily x:Key="FontFamilySansSerif">Arial</FontFamily>
    <FontFamily x:Key="FontFamilySerif">Georgia</FontFamily>
    <FontFamily x:Key="FontFamilyMonospace">Consolas</FontFamily>
    <FontFamily x:Key="FontFamilyBase">Arial</FontFamily>

    <sys:Double x:Key="FontSizeBase">14</sys:Double>
    <sys:Double x:Key="FontSizeLarge">18</sys:Double>
    <sys:Double x:Key="FontSizeSmall">12</sys:Double>

    <sys:Double x:Key="FontSizeH1">36</sys:Double>
    <sys:Double x:Key="FontSizeH2">30</sys:Double>
    <sys:Double x:Key="FontSizeH3">24</sys:Double>
    <sys:Double x:Key="FontSizeH4">18</sys:Double>
    <sys:Double x:Key="FontSizeH5">14</sys:Double>
    <sys:Double x:Key="FontSizeH6">12</sys:Double>

    <sys:Double x:Key="LineHeightBaseFactor">1.428571429</sys:Double>

    <!-- Components -->

    <sys:Double x:Key="PaddingBaseVertical">6</sys:Double>
    <sys:Double x:Key="PaddingBaseHorizontal">12</sys:Double>

    <CornerRadius x:Key="BorderRadiusBase" TopLeft="4" TopRight="4" BottomRight="4" BottomLeft="4" />

    <!-- Buttons -->

    <Style x:Key="ButtonMediumDefaultStyle" TargetType="{x:Type Button}" BasedOn="{StaticResource {x:Type Button}}">
        <Style.Resources>
            <SolidColorBrush x:Key="ButtonDefaultColorBrush" Color="#FF333333" />
            <SolidColorBrush x:Key="ButtonDefaultBackgroundBrush" Color="#FFFFFFFF" />
            <SolidColorBrush x:Key="ButtonDefaultBorderBrush" Color="#FFCCCCCC" />
        </Style.Resources>

        <Setter Property="Border.CornerRadius" Value="{StaticResource BorderRadiusBase}" />
        <Setter Property="HorizontalContentAlignment" Value="Center" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="FontWeight" Value="Regular" />
        <Setter Property="FontSize" Value="{StaticResource FontSizeBase}" />
        <Setter Property="FontFamily" Value="{StaticResource FontFamilyBase}" />
        <Setter Property="TextBlock.LineHeight" Value="20" />
        <Setter Property="Padding" Value="12,8,12,4" />
        <Setter Property="Foreground" Value="{StaticResource ButtonDefaultColorBrush}" />
        <Setter Property="Background" Value="{StaticResource ButtonDefaultBackgroundBrush}" />
        <Setter Property="BorderBrush" Value="{StaticResource ButtonDefaultBorderBrush}" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="Focusable" Value="False" />

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Button}">
                    <Grid SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}">
                        <Grid.Resources>
                            <SolidColorBrush x:Key="BackgroundHover" Color="{Binding Background.(SolidColorBrush.Color), RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource DarkenConverter}, ConverterParameter=10, Mode=OneTime}" />
                            <SolidColorBrush x:Key="BorderBrushHover" Color="{Binding BorderBrush.(SolidColorBrush.Color), RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource DarkenConverter}, ConverterParameter=12, Mode=OneTime}" />
                            <SolidColorBrush x:Key="BackgroundPressed" Color="{Binding Background.(SolidColorBrush.Color), RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource DarkenConverter}, ConverterParameter=17, Mode=OneTime}" />
                            <SolidColorBrush x:Key="BorderBrushPressed" Color="{Binding BorderBrush.(SolidColorBrush.Color), RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource DarkenConverter}, ConverterParameter=25, Mode=OneTime}" />
                        </Grid.Resources>
                        <Border x:Name="PART_Background" CornerRadius="{TemplateBinding Border.CornerRadius}" Background="{TemplateBinding Background}" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" ClipToBounds="True">
                            <Border
                                x:Name="PART_Border"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                CornerRadius="{TemplateBinding Border.CornerRadius}"
                                Background="Transparent"
                                Padding="{TemplateBinding Padding}"
                                SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}">
                                <Border.Effect>
                                    <DropShadowEffect ShadowDepth="0" BlurRadius="5" RenderingBias="Performance" Opacity="0" />
                                </Border.Effect>
                                <ContentPresenter
                                    TextBlock.Foreground="{TemplateBinding Foreground}"
                                    TextBlock.LineHeight="{TemplateBinding TextBlock.LineHeight}"
                                    TextBlock.FontSize="{TemplateBinding FontSize}"
                                    TextBlock.FontFamily="{TemplateBinding FontFamily}"
                                    TextBlock.FontWeight="{TemplateBinding FontWeight}"
                                    HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                    VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                    Content="{TemplateBinding Content}"
                                    ContentTemplate="{TemplateBinding ContentTemplate}"
                                    SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                            </Border>
                        </Border>

                        <VisualStateManager.VisualStateGroups>
                            <VisualStateGroup x:Name="CommonStates">
                                <VisualState x:Name="Normal" />
                                <VisualState x:Name="MouseOver">
                                    <Storyboard>
                                        <ColorAnimation
                                            Storyboard.TargetName="PART_Background"
                                            Storyboard.TargetProperty="Background.(SolidColorBrush.Color)"
                                            To="{Binding Color, Source={StaticResource BackgroundHover}, Mode=OneTime}"
                                            Duration="0" />
                                        <ColorAnimation
                                            Storyboard.TargetName="PART_Border"
                                            Storyboard.TargetProperty="BorderBrush.(SolidColorBrush.Color)"
                                            To="{Binding Color, Source={StaticResource BorderBrushHover}, Mode=OneTime}"
                                            Duration="0" />
                                    </Storyboard>
                                </VisualState>
                                <VisualState x:Name="Pressed">
                                    <Storyboard>
                                        <ColorAnimation
                                            Storyboard.TargetName="PART_Background"
                                            Storyboard.TargetProperty="Background.(SolidColorBrush.Color)"
                                            To="{Binding Color, Source={StaticResource BackgroundPressed}, Mode=OneTime}"
                                            Duration="0" />
                                        <ColorAnimation
                                            Storyboard.TargetName="PART_Border"
                                            Storyboard.TargetProperty="BorderBrush.(SolidColorBrush.Color)"
                                            To="{Binding Color, Source={StaticResource BorderBrushPressed}, Mode=OneTime}"
                                            Duration="0" />
                                        <DoubleAnimation
                                            Storyboard.TargetName="PART_Border"
                                            Storyboard.TargetProperty="Effect.(DropShadowEffect.Opacity)"
                                            To="0.125"
                                            Duration="0" />
                                    </Storyboard>
                                </VisualState>
                                <VisualState x:Name="Disabled">
                                    <Storyboard>
                                        <DoubleAnimation
                                            Storyboard.TargetName="PART_Background"
                                            Storyboard.TargetProperty="Opacity"
                                            To="0.6"
                                            Duration="0" />
                                    </Storyboard>
                                </VisualState>
                            </VisualStateGroup>
                        </VisualStateManager.VisualStateGroups>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="LinkButtonMediumDefaultStyle" TargetType="{x:Type Button}" BasedOn="{StaticResource ButtonMediumDefaultStyle}">
        <Setter Property="BorderThickness" Value="0" />
    </Style>

    <Style x:Key="ButtonMediumPrimaryStyle" TargetType="{x:Type Button}" BasedOn="{StaticResource ButtonMediumDefaultStyle}">
        <Style.Resources>
            <SolidColorBrush x:Key="ButtonPrimaryColorBrush" Color="#FFFFFFFF" />
            <SolidColorBrush x:Key="ButtonPrimaryBackgroundBrush" Color="{Binding Color, Source={StaticResource BrandPrimaryBrush}, Mode=OneTime}" />
            <SolidColorBrush x:Key="ButtonPrimaryBorderBrush" Color="{Binding Color, Source={StaticResource ButtonPrimaryBackgroundBrush}, Converter={StaticResource DarkenConverter}, ConverterParameter=5, Mode=OneTime}" />
        </Style.Resources>

        <Setter Property="Foreground" Value="{StaticResource ButtonPrimaryColorBrush}" />
        <Setter Property="Background" Value="{StaticResource ButtonPrimaryBackgroundBrush}" />
        <Setter Property="BorderBrush" Value="{StaticResource ButtonPrimaryBorderBrush}" />
    </Style>

    <Style x:Key="ButtonMediumSuccessStyle" TargetType="{x:Type Button}" BasedOn="{StaticResource ButtonMediumDefaultStyle}">
        <Style.Resources>
            <SolidColorBrush x:Key="ButtonSuccessColorBrush" Color="#FFFFFFFF" />
            <SolidColorBrush x:Key="ButtonSuccessBackgroundBrush" Color="{Binding Color, Source={StaticResource BrandSuccessBrush}, Mode=OneTime}" />
            <SolidColorBrush x:Key="ButtonSuccessBorderBrush" Color="{Binding Color, Source={StaticResource ButtonSuccessBackgroundBrush}, Converter={StaticResource DarkenConverter}, ConverterParameter=5, Mode=OneTime}" />
        </Style.Resources>

        <Setter Property="Foreground" Value="{StaticResource ButtonSuccessColorBrush}" />
        <Setter Property="Background" Value="{StaticResource ButtonSuccessBackgroundBrush}" />
        <Setter Property="BorderBrush" Value="{StaticResource ButtonSuccessBorderBrush}" />
    </Style>

    <Style x:Key="LinkButtonMediumPrimaryStyle" TargetType="{x:Type Button}" BasedOn="{StaticResource LinkButtonMediumDefaultStyle}">
        <Setter Property="Foreground" Value="{StaticResource BrandPrimaryBrush}" />
    </Style>

</ResourceDictionary>